BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[HRUpdates] ALTER COLUMN [Title] VARCHAR(50) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
